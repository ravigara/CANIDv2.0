# Dog Nose Biometric Identification System - requirements
# Python 3.10+ recommended.
#
# NOTE on GPU: an NVIDIA RTX 5050 Laptop GPU is a Blackwell (sm_120) part.
# Stable CPU wheels work everywhere, but for GPU acceleration you need the
# CUDA 12.8 build of PyTorch. Install torch FIRST, separately, e.g.:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# Then:  pip install -r requirements.txt

# --- core (always required; everything below torch) ---
numpy>=1.26,<2.3
opencv-python>=4.8
pillow>=10.0
scipy>=1.11
scikit-learn>=1.3
scikit-image>=0.22
PyYAML>=6.0
tqdm>=4.66

# --- metric-learning stack ---
# torch + torchvision installed separately via the cu128 wheel (see header)
torch>=2.5
torchvision>=0.20

# --- detection ---
ultralytics>=8.3.0          # YOLOv11/YOLOv12 trainer + inference

# --- segmentation ---
transformers>=4.40          # SegFormer
timm>=1.0                   # ConvNeXt-V2 / EfficientNetV2 backbones
segmentation-models-pytorch>=0.4  # U-Net fallback

# --- augmentation ---
albumentations>=2.0

# --- vector database ---
faiss-cpu>=1.8              # swap for faiss-gpu-cu12 on a supported box

# --- serving / export ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9     # FastAPI file uploads
onnx>=1.16
onnxruntime>=1.17

# --- mobile deployment (optional; heavy) ---
# tensorflow>=2.16           # TFLite conversion (Stage 13/14)
# ai-edge-litert>=1.0        # lighter TFLite runtime alternative

# --- dev / quality ---
pytest>=8.0
matplotlib>=3.8             # explainability overlays
